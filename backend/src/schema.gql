# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type AuthPayload {
  """Short-lived JWT to send as `Authorization: Bearer`"""
  accessToken: String!
  user: User!
}

type CateringOffer {
  commentsCount: Int!
  createdAt: DateTime!

  """Optional longer description (what the menu includes)."""
  description: String
  id: ID!

  """Photos of the offer, in upload order."""
  images: [CateringOfferImage!]!

  """
  Kind label from the frontend catalog (e.g. "FURSHET"), mirroring Design.occasion.
  """
  kind: String!
  likedByMe: Boolean!
  likesCount: Int!

  """The catering page that published it."""
  page: Page

  """Optional per-guest price in GEL."""
  pricePerPerson: Int

  """Title of the offer."""
  title: String!
  updatedAt: DateTime!
}

type CateringOfferImage {
  id: ID!

  """Position in the upload order."""
  order: Int!

  """URL of the photo, served from /uploads."""
  url: String!
}

input ChangePasswordInput {
  currentPassword: String
  newPassword: String!
}

type Comment {
  """The user who wrote the comment."""
  author: User!

  """Text of the comment."""
  body: String!
  createdAt: DateTime!
  id: ID!
}

enum ContentTarget {
  CATERING_OFFER
  DESIGN
  EVENT
  FLORIST_ITEM
  OFFERING
  POST
  REQUEST
  WORK
}

input CreateCateringOfferInput {
  description: String

  """Photo URLs returned by the /uploads endpoint, in display order."""
  imageUrls: [String!]!

  """Kind label from the frontend catalog (e.g. "FURSHET")."""
  kind: String!

  """The CATERING page to publish under (you must own it)."""
  pageId: ID!

  """Optional per-guest price in GEL."""
  pricePerPerson: Int

  """Title of the offer."""
  title: String!
}

input CreateCommentInput {
  """Text of the comment."""
  body: String!

  """What kind of content the comment is on."""
  target: ContentTarget!

  """Id of the content being commented on."""
  targetId: ID!
}

input CreateDesignInput {
  description: String

  """Photo URLs returned by the /uploads endpoint, in display order."""
  imageUrls: [String!]!

  """Occasion label from the frontend catalog (e.g. "WEDDING")."""
  occasion: String!

  """The DESIGNER page to publish under (you must own it)."""
  pageId: ID!

  """Optional starting price in GEL."""
  priceFrom: Int

  """Title of the design."""
  title: String!
}

input CreateEventInput {
  """Cover photo URL returned by the /uploads endpoint."""
  coverUrl: String
  description: String
  location: String

  """
  Host the event as this page (you must own it). Omit for a personal event.
  """
  pageId: ID

  """When the event takes place."""
  startsAt: DateTime!

  """Second-level category within `type` (e.g. "TENNIS" for SPORT)."""
  subtype: String

  """Title of the event."""
  title: String!

  """Category of the event. Defaults to OTHER if omitted."""
  type: EventType
}

input CreateFloristItemInput {
  description: String

  """Photo URLs returned by the /uploads endpoint, in display order."""
  imageUrls: [String!]!

  """Kind label from the frontend catalog (e.g. "WEDDING_BOUQUET")."""
  kind: String!

  """The FLORIST page to publish under (you must own it)."""
  pageId: ID!

  """Optional starting price in GEL."""
  priceFrom: Int

  """Title of the arrangement."""
  title: String!
}

input CreateOfferingInput {
  description: String

  """Photo URLs returned by the /uploads endpoint, in display order."""
  imageUrls: [String!]!

  """
  Kind label from the frontend catalog (e.g. "SAXOPHONE", "SOUND_SYSTEM").
  """
  kind: String!

  """The MUSIC_SOUND page to publish under (you must own it)."""
  pageId: ID!

  """Optional starting price in GEL."""
  priceFrom: Int

  """Title of the offering."""
  title: String!
}

input CreatePageInput {
  """Display name of the page."""
  name: String!

  """URL returned by the /uploads endpoint for the page photo."""
  photoUrl: String

  """Vendor categories — at least one, no duplicates."""
  types: [PageType!]!
}

input CreatePostInput {
  body: String
  media: [PostMediaInput!]

  """
  Publish the post as this page (you must own it). Omit for a personal post.
  """
  pageId: ID
}

input CreateRequestInput {
  """Free-text description of what is needed."""
  body: String!

  """Budget lower bound, GEL."""
  budgetFrom: Int

  """Budget upper bound, GEL."""
  budgetTo: Int

  """Which vendor category this request targets."""
  category: PageType!
  city: String

  """When the event takes place."""
  eventDate: DateTime

  """Occasion label from the frontend catalog (e.g. "WEDDING")."""
  occasion: String
}

input CreateWorkInput {
  """Category label from the frontend catalog (e.g. "WEDDING")."""
  category: String!
  description: String

  """Photos/videos in display order."""
  media: [WorkMediaInput!]!

  """The Photo & Video page to publish under (you must own it)."""
  pageId: ID!

  """Optional starting price in GEL."""
  priceFrom: Int

  """Title of the work."""
  title: String!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Design {
  commentsCount: Int!
  createdAt: DateTime!

  """Optional longer description."""
  description: String
  id: ID!

  """Photos of the design, in upload order."""
  images: [DesignImage!]!
  likedByMe: Boolean!
  likesCount: Int!

  """
  Occasion label from the frontend catalog (e.g. "WEDDING"), mirroring Event.subtype.
  """
  occasion: String

  """The designer page that published it."""
  page: Page

  """Optional starting price in GEL (shown as "from X ₾")."""
  priceFrom: Int

  """Title of the design."""
  title: String!
  updatedAt: DateTime!
}

type DesignImage {
  id: ID!

  """Position in the upload order."""
  order: Int!

  """URL of the photo, served from /uploads."""
  url: String!
}

type Event {
  """A few users who are going (for the avatar row)."""
  attendees: [User!]!

  """The user who created the event."""
  author: User!
  commentsCount: Int!

  """URL of the cover photo, served from /uploads."""
  coverUrl: String
  createdAt: DateTime!

  """Optional longer description."""
  description: String

  """How many users are going."""
  goingCount: Int!
  id: ID!

  """How many users are interested."""
  interestedCount: Int!
  likedByMe: Boolean!
  likesCount: Int!

  """Optional human-readable location."""
  location: String

  """The signed-in viewer's RSVP (null when none)."""
  myRsvp: RsvpStatus

  """Set when the event is hosted as a page; null for a personal event."""
  page: Page

  """When the event takes place."""
  startsAt: DateTime!

  """
  Second-level category within `type` (e.g. "TENNIS" for SPORT); null when the type has no subtypes.
  """
  subtype: String

  """Title of the event."""
  title: String!

  """Category of the event."""
  type: EventType!
  updatedAt: DateTime!
}

enum EventType {
  BIRTHDAY
  CAMPING
  HIKING
  OTHER
  PARTY
  SPORT
}

"""Anything that can appear in the news feed."""
union FeedItem = CateringOffer | Design | Event | FloristItem | Offering | Post | Request | Work

type FeedPage {
  """Whether more stories exist after this page."""
  hasMore: Boolean!

  """Feed stories, newest first."""
  items: [FeedItem!]!

  """
  Pass as `cursor` to fetch the next page; null when this is the last page.
  """
  nextCursor: DateTime
}

type FloristItem {
  commentsCount: Int!
  createdAt: DateTime!

  """Optional longer description."""
  description: String
  id: ID!

  """Photos of the arrangement, in upload order."""
  images: [FloristItemImage!]!

  """
  Kind label from the frontend catalog (e.g. "WEDDING_BOUQUET"), mirroring Design.occasion.
  """
  kind: String!
  likedByMe: Boolean!
  likesCount: Int!

  """The florist page that published it."""
  page: Page

  """Optional starting price in GEL."""
  priceFrom: Int

  """Title of the arrangement."""
  title: String!
  updatedAt: DateTime!
}

type FloristItemImage {
  id: ID!

  """Position in the upload order."""
  order: Int!

  """URL of the photo, served from /uploads."""
  url: String!
}

type LikeResult {
  likedByMe: Boolean!
  likesCount: Int!

  """The content that was (un)liked."""
  targetId: ID!
}

input LoginInput {
  email: String!
  password: String!
}

input LoginWithCodeInput {
  code: String!
  email: String!
}

enum MediaType {
  IMAGE
  VIDEO
}

type Mutation {
  """Write a comment on content."""
  addComment(input: CreateCommentInput!): Comment!

  """
  Change the signed-in user's password (confirms the current one unless the account has none).
  """
  changePassword(input: ChangePasswordInput!): Boolean!

  """Publish a menu offer under one of your catering pages."""
  createCateringOffer(input: CreateCateringOfferInput!): CateringOffer!

  """Publish a design under one of your designer pages."""
  createDesign(input: CreateDesignInput!): Design!

  """Create an event."""
  createEvent(input: CreateEventInput!): Event!

  """Publish an arrangement under one of your florist pages."""
  createFloristItem(input: CreateFloristItemInput!): FloristItem!

  """Publish an offering under one of your musician or equipment pages."""
  createOffering(input: CreateOfferingInput!): Offering!

  """Create a page with a name, type, and optional photo."""
  createPage(input: CreatePageInput!): Page!

  """Create a post with text and/or uploaded media."""
  createPost(input: CreatePostInput!): Post!

  """Post a demand request ("I need a photographer for ...")."""
  createRequest(input: CreateRequestInput!): Request!

  """Publish a portfolio work under one of your photo & video pages."""
  createWork(input: CreateWorkInput!): Work!

  """Delete a menu offer from one of your own pages."""
  deleteCateringOffer(id: ID!): Boolean!

  """Delete one of your own comments."""
  deleteComment(id: ID!): Boolean!

  """Delete a design from one of your own pages."""
  deleteDesign(id: ID!): Boolean!

  """Delete one of your own events."""
  deleteEvent(id: ID!): Boolean!

  """Delete a catalog item from one of your own pages."""
  deleteFloristItem(id: ID!): Boolean!

  """Delete an offering from one of your own pages."""
  deleteOffering(id: ID!): Boolean!

  """Delete one of your own pages."""
  deletePage(id: ID!): Boolean!

  """Delete one of your own posts."""
  deletePost(id: ID!): Boolean!

  """Delete one of your own requests."""
  deleteRequest(id: ID!): Boolean!

  """Delete a work from one of your own pages."""
  deleteWork(id: ID!): Boolean!

  """Follow another user."""
  follow(userId: ID!): User!

  """Follow a page."""
  followPage(pageId: ID!): Page!

  """Like a piece of content."""
  like(target: ContentTarget!, targetId: ID!): LikeResult!
  login(input: LoginInput!): AuthPayload!

  """Sign in by confirming a one-time code emailed via requestLoginCode."""
  loginWithCode(input: LoginWithCodeInput!): AuthPayload!
  logout: Boolean!

  """
  Exchange the refresh token (read from the httpOnly cookie) for a new access token, rotating the refresh token.
  """
  refreshToken: AuthPayload!

  """
  Create a new account and email a verification code. The account cannot sign in until the code is confirmed via verifyEmail.
  """
  register(input: RegisterInput!): User!

  """
  Email a one-time sign-in code (rate-limited). Always returns true, even for unknown emails.
  """
  requestLoginCode(email: String!): Boolean!

  """
  Email a password-reset code (rate-limited). Always returns true, even for unknown emails.
  """
  requestPasswordReset(email: String!): Boolean!

  """
  Re-send a verification code to an unverified account (rate-limited). Always returns true.
  """
  resendVerificationCode(email: String!): Boolean!

  """
  Confirm a reset code and set a new password. Revokes all existing sessions; the user must sign in again.
  """
  resetPassword(input: ResetPasswordInput!): Boolean!

  """
  Set your RSVP on an event: GOING or INTERESTED; omit status to withdraw.
  """
  rsvpEvent(eventId: ID!, status: RsvpStatus): Event!

  """Unfollow a user."""
  unfollow(userId: ID!): User!

  """Unfollow a page."""
  unfollowPage(pageId: ID!): Page!

  """Remove your like from content."""
  unlike(target: ContentTarget!, targetId: ID!): LikeResult!

  """Edit one of your menu offers."""
  updateCateringOffer(input: UpdateCateringOfferInput!): CateringOffer!

  """Edit one of your designs."""
  updateDesign(input: UpdateDesignInput!): Design!

  """Edit one of your own events."""
  updateEvent(input: UpdateEventInput!): Event!

  """Edit one of your catalog items."""
  updateFloristItem(input: UpdateFloristItemInput!): FloristItem!

  """Edit one of your offerings."""
  updateOffering(input: UpdateOfferingInput!): Offering!

  """Update one of your own pages (name, types, photo, contact links)."""
  updatePage(input: UpdatePageInput!): Page!

  """Update the signed-in user's profile."""
  updateProfile(input: UpdateProfileInput!): User!

  """Edit one of your works."""
  updateWork(input: UpdateWorkInput!): Work!

  """
  Confirm the emailed verification code. On success the account is verified and signed in.
  """
  verifyEmail(input: VerifyEmailInput!): AuthPayload!
}

type Offering {
  commentsCount: Int!
  createdAt: DateTime!

  """Optional longer description."""
  description: String
  id: ID!

  """Photos of the offering, in upload order."""
  images: [OfferingImage!]!

  """
  Kind label from the frontend catalog (e.g. "SAXOPHONE", "SOUND_SYSTEM").
  """
  kind: String!
  likedByMe: Boolean!
  likesCount: Int!

  """The page that published it."""
  page: Page

  """Optional starting price in GEL (shown as "from X ₾")."""
  priceFrom: Int

  """Title of the offering."""
  title: String!
  updatedAt: DateTime!
}

type OfferingImage {
  id: ID!

  """Position in the upload order."""
  order: Int!

  """URL of the photo, served from /uploads."""
  url: String!
}

type Page {
  createdAt: DateTime!

  """Number of users following this page."""
  followersCount: Int!
  id: ID!

  """
  What a MUSIC_SOUND page plays — ids from the frontend instruments catalog (e.g. "SAXOPHONE"). Empty for other pages.
  """
  instruments: [String!]!

  """
  Whether the signed-in viewer follows this page (false when signed out).
  """
  isFollowedByMe: Boolean!

  """Display name of the page."""
  name: String!

  """The user who created and owns the page."""
  owner: User!

  """Contact phone, rendered as a tel: link."""
  phone: String

  """URL of the page photo, served from /uploads."""
  photoUrl: String

  """Number of posts published by this page."""
  postsCount: Int!

  """Telegram username without the @ (t.me link)."""
  telegram: String

  """
  Vendor categories of the page — one or more (a page can be e.g. both a photographer and light & sound).
  """
  types: [PageType!]!
  updatedAt: DateTime!

  """WhatsApp number in international format (wa.me link)."""
  whatsapp: String
}

enum PageType {
  CATERING
  DESIGNER
  FLORIST
  MUSIC_SOUND
  PHOTOGRAPHER
}

type Post {
  """The user who created the post."""
  author: User!

  """Optional text body."""
  body: String
  commentsCount: Int!
  createdAt: DateTime!
  id: ID!
  likedByMe: Boolean!
  likesCount: Int!

  """Attached images and videos."""
  media: [PostMedia!]!

  """Set when the post was published as a page; null for personal posts."""
  page: Page
  updatedAt: DateTime!
}

type PostMedia {
  createdAt: DateTime!
  id: ID!
  type: MediaType!

  """URL the uploaded file is served from."""
  url: String!
}

input PostMediaInput {
  type: MediaType!

  """URL returned by the /uploads endpoint."""
  url: String!
}

type Query {
  """
  Browse menu offers across all catering pages, newest first, optionally filtered by kind.
  """
  cateringOffers(kind: String): [CateringOffer!]!

  """All comments on a piece of content, oldest first."""
  comments(target: ContentTarget!, targetId: ID!): [Comment!]!

  """
  Browse designs across all designer pages, newest first, optionally filtered by occasion.
  """
  designs(occasion: String): [Design!]!

  """A single event by id."""
  event(id: ID!): Event!

  """Global events feed: everyone's events, newest first."""
  eventsFeed: [Event!]!

  """Global news feed: everyone's posts, newest first."""
  feed: [Post!]!

  """
  Unified news feed: posts, events, and all vendor content, newest first, cursor-paginated.
  """
  feedPage(cursor: DateTime, limit: Int): FeedPage!

  """
  Browse arrangements across all florist pages, newest first, optionally filtered by kind.
  """
  floristItems(kind: String): [FloristItem!]!
  me: User!

  """The signed-in user's own pages, newest first."""
  myPages: [Page!]!

  """
  Browse offerings across all musician/equipment pages, newest first, optionally filtered by kind.
  """
  offerings(kind: String): [Offering!]!

  """Fetch a single page by id."""
  page(id: ID!): Page!

  """A page's menu offers, newest first."""
  pageCateringOffers(pageId: ID!): [CateringOffer!]!

  """A page's designs, newest first."""
  pageDesigns(pageId: ID!): [Design!]!

  """A page's events, soonest first."""
  pageEvents(pageId: ID!): [Event!]!

  """A page's catalog items, newest first."""
  pageFloristItems(pageId: ID!): [FloristItem!]!

  """A page's offerings, newest first."""
  pageOfferings(pageId: ID!): [Offering!]!

  """A page's posts, newest first."""
  pagePosts(pageId: ID!): [Post!]!

  """A page's portfolio works, newest first."""
  pageWorks(pageId: ID!): [Work!]!

  """Browse all pages, newest first."""
  pages: [Page!]!

  """
  Browse demand requests, newest first, optionally filtered by vendor category.
  """
  requests(category: PageType): [Request!]!

  """
  Requests matching the categories of the viewer's pages — the vendor inbox.
  """
  requestsForMyPages: [Request!]!

  """Search pages by name (case-insensitive)."""
  searchPages(query: String!): [Page!]!

  """Search users by @username or name (excludes the viewer)."""
  searchUsers(query: String!): [User!]!

  """Fetch a user by their @username."""
  user(username: String!): User!

  """A user's events, soonest first."""
  userEvents(username: String!): [Event!]!

  """A user's posts, newest first."""
  userPosts(username: String!): [Post!]!

  """
  Browse works across all photo & video pages, newest first, optionally filtered by category.
  """
  works(category: String): [Work!]!
}

input RegisterInput {
  birthDate: String!
  email: String!
  firstName: String!
  lastName: String!
  password: String!
  username: String!
}

type Request {
  """The user who posted the request."""
  author: User!

  """Free-text description of what is needed."""
  body: String

  """Budget lower bound in GEL, when given."""
  budgetFrom: Int

  """Budget upper bound in GEL, when given."""
  budgetTo: Int

  """Which vendor category this request targets."""
  category: PageType!

  """City, when given."""
  city: String
  commentsCount: Int!
  createdAt: DateTime!

  """When the event takes place, when given."""
  eventDate: DateTime
  id: ID!
  likedByMe: Boolean!
  likesCount: Int!

  """Occasion label from the frontend catalog (e.g. "WEDDING"), when given."""
  occasion: String
  updatedAt: DateTime!
}

input ResetPasswordInput {
  code: String!
  email: String!
  newPassword: String!
}

enum Role {
  ADMIN
  USER
}

enum RsvpStatus {
  GOING
  INTERESTED
}

input UpdateCateringOfferInput {
  """New description; empty string clears it."""
  description: String

  """Replaces the whole photo list when provided."""
  imageUrls: [String!]
  kind: String

  """The offer to update (you must own its page)."""
  offerId: ID!

  """New per-guest price in GEL; -1 clears it."""
  pricePerPerson: Int
  title: String
}

input UpdateDesignInput {
  """New description; empty string clears it."""
  description: String

  """The design to update (you must own its page)."""
  designId: ID!

  """Replaces the whole photo list when provided."""
  imageUrls: [String!]
  occasion: String

  """New starting price in GEL; -1 clears it."""
  priceFrom: Int
  title: String
}

input UpdateEventInput {
  """New cover URL; empty string removes the cover."""
  coverUrl: String

  """New description; empty string clears it."""
  description: String

  """The event to update (you must be its author)."""
  eventId: ID!

  """New location; empty string clears it."""
  location: String
  startsAt: DateTime

  """New subtype; empty string clears it."""
  subtype: String
  title: String
  type: EventType
}

input UpdateFloristItemInput {
  """New description; empty string clears it."""
  description: String

  """Replaces the whole photo list when provided."""
  imageUrls: [String!]

  """The item to update (you must own its page)."""
  itemId: ID!
  kind: String

  """New starting price in GEL; -1 clears it."""
  priceFrom: Int
  title: String
}

input UpdateOfferingInput {
  """New description; empty string clears it."""
  description: String

  """Replaces the whole photo list when provided."""
  imageUrls: [String!]
  kind: String

  """The offering to update (you must own its page)."""
  offeringId: ID!

  """New starting price in GEL; -1 clears it."""
  priceFrom: Int
  title: String
}

input UpdatePageInput {
  """
  Instruments a MUSIC_SOUND page plays (catalog ids, e.g. "SAXOPHONE"); pass an empty list to clear.
  """
  instruments: [String!]

  """New display name."""
  name: String

  """The page to update (you must own it)."""
  pageId: ID!

  """Contact phone; empty string clears it."""
  phone: String

  """New photo URL from /uploads; pass an empty string to remove the photo."""
  photoUrl: String

  """Telegram username (with or without @); empty string clears it."""
  telegram: String

  """New vendor categories — at least one, no duplicates."""
  types: [PageType!]

  """WhatsApp number; empty string clears it."""
  whatsapp: String
}

input UpdateProfileInput {
  avatarUrl: String
  bio: String
  birthDate: String
  firstName: String
  lastName: String
  location: String
  username: String
}

input UpdateWorkInput {
  category: String

  """New description; empty string clears it."""
  description: String

  """Replaces the whole media list when provided."""
  media: [WorkMediaInput!]

  """New starting price in GEL; -1 clears it."""
  priceFrom: Int
  title: String

  """The work to update (you must own its page)."""
  workId: ID!
}

type User {
  avatarUrl: String
  bio: String
  birthDate: DateTime
  createdAt: DateTime!
  email: String!
  firstName: String

  """Users who follow this user."""
  followers: [User!]!

  """Number of users following this user."""
  followersCount: Int!

  """Users this user follows."""
  following: [User!]!

  """Number of users this user follows."""
  followingCount: Int!
  id: ID!
  isActive: Boolean!

  """Whether the signed-in viewer follows this user (false for self)."""
  isFollowedByMe: Boolean!
  isVerified: Boolean!
  lastName: String
  location: String

  """Thematic pages this user owns."""
  pages: [Page!]!
  role: Role!
  updatedAt: DateTime!
  username: String
}

input VerifyEmailInput {
  code: String!
  email: String!
}

type Work {
  """
  Category label from the frontend catalog (e.g. "WEDDING"), mirroring Design.occasion.
  """
  category: String!
  commentsCount: Int!
  createdAt: DateTime!

  """Optional longer description."""
  description: String
  id: ID!
  likedByMe: Boolean!
  likesCount: Int!

  """Photos and videos of the work, in upload order."""
  media: [WorkMedia!]!

  """The page that published it."""
  page: Page

  """Optional starting price in GEL."""
  priceFrom: Int

  """Title of the work."""
  title: String!
  updatedAt: DateTime!
}

type WorkMedia {
  id: ID!

  """Position in the upload order."""
  order: Int!

  """Whether this is a photo or video."""
  type: MediaType!

  """URL of the file, served from /uploads."""
  url: String!
}

input WorkMediaInput {
  type: MediaType!

  """URL returned by the /uploads endpoint."""
  url: String!
}